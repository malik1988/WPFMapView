<UserControl x:Class="MAP.MapViewObjects.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel >
            <Viewbox Height="40">
                <TextBlock Text="地图名" x:Name="label地图名" FontWeight="Bold" Foreground="OrangeRed"/>
            </Viewbox>
            <!--<Line  X1="0" Y1="0" X2="{Binding ElementName=self,Path=Width}" Y2="0" Stroke="Blue" StrokeThickness="2"/>-->
            <Viewbox>
                <Border BorderThickness="1" BorderBrush="DarkGreen" >
                    <Canvas x:Name="mapCanvas" Margin="2"/>
                </Border>
            </Viewbox>


        </StackPanel>
    </Grid>
</UserControl>
